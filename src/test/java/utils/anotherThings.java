package utils;

import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebElement;

public class anotherThings {
//    WebElement hotelTitle = driver.findElement(By.xpath("//div[@id='favorites']//li//div[text()= 'Hotel Brasov']"));
//        Actions hover = new Actions(driver);
//        Action mouseOver = hover.moveToElement(hotelTitle).build();
//        mouseOver.perform();

 //       Assert.assertEquals("Another hotel.", "Hotel Brasov", hotelTitle.getText());
}
// Thread.sleep(100000);


//    WebElement favoritesSection = driver.findElement
//            (By.xpath("//div[@class = 'p-2 text-xl font-bold lg:text-2xl' ][text() = 'Favorites']"));
//
//    JavascriptExecutor js = (JavascriptExecutor) driver;
////js.executeScript("arguments[0].scrollIntoView();", favoritesSection);
//        js.executeScript("window.scrollTo(0, document.body.scrollHeight)");
//                driver.manage().timeouts().implicitlyWait(5, TimeUnit.SECONDS);

//js.executeScript("arguments[0].scrollIntoView();", logoutButton);